{% if not row.system %}
    {% include '@MainBundle/admin/partials/delete_confirmation.html.twig' with {
        'title': 'zentlix_main.delete',
        'confirmation': 'zentlix_main.bundle.delete.confirmation',
        'path': path('admin.bundle.remove', {'id': row.id}),
        'class': ''
    } %}
{% endif %}