{{ form_start(form, {'action': path('admin.attribute.update', {'id': attribute.id})}) }}
    {{ form_widget(form) }}
    <div class="menu-save-item">
        <button class="btn btn-sm btn-primary" type="submit">
            <i class="fa fa-dot-circle-o"></i> {{ 'zentlix_main.save' | trans }}
        </button>
        {% include '@MainBundle/admin/partials/delete_confirmation.html.twig' with {
            'title': 'zentlix_main.attribute.delete.action',
            'confirmation': 'zentlix_main.attribute.delete.confirmation',
            'path': path('admin.attribute.delete', {'id': attribute.id})
        } %}
    </div>
{{ form_end(form) }}